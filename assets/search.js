fetch("search-index.json").then(r=>r.json()).then(index=>{const q=document.querySelector("#q"),out=document.querySelector("#results");q.addEventListener("input",()=>{const term=q.value.trim().toLowerCase();if(!term){out.innerHTML="";return}const hits=index.filter(x=>x.terms.some(t=>String(t).toLowerCase().includes(term))).slice(0,100);out.innerHTML=hits.map(x=>`<section><small>${x.type}</small><h3><a href="${x.url}">${x.title}</a></h3></section>`).join("")})});
