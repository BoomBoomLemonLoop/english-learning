(()=>{let last="",voices=[];const voice=document.querySelector("#voice"),rate=document.querySelector("#rate");function load(){voices=speechSynthesis.getVoices().filter(v=>v.lang.toLowerCase().startsWith("en"));if(voice)voice.innerHTML=voices.map((v,i)=>`<option value="${i}">${v.name} (${v.lang})</option>`).join("")}function speak(text){last=text;speechSynthesis.cancel();const u=new SpeechSynthesisUtterance(text);u.lang="en";u.rate=Number(rate?.value||1);if(voices[Number(voice?.value||0)])u.voice=voices[Number(voice.value)];speechSynthesis.speak(u)}speechSynthesis.onvoiceschanged=load;load();document.addEventListener("click",e=>{const b=e.target.closest("[data-tts]");if(b)speak(b.dataset.tts);if(e.target.id==="stop")speechSynthesis.cancel();if(e.target.id==="replay"&&last)speak(last)});})();
